# ── Source du corpus ──────────────────────────────────────────────
# Il n'y a pas de base de données : la source de vérité est le fichier
# corpus/source.json, dans le même stockage que les médias.

# ── Accès au studio ───────────────────────────────────────────────
# Empreinte scrypt du mot de passe unique — le studio n'a pas d'autre
# utilisateur. Le mot de passe lui-même n'est jamais stocké : une fuite des
# variables d'environnement ne le révèle donc pas.
#
# Générer avec :
#   pnpm tsx seed/hacher.ts 'mon mot de passe'
STUDIO_MOT_DE_PASSE_HACHE=

# Clé de signature du cookie de session. Générer avec :
#   openssl rand -base64 32
SESSION_SECRET=change-moi-aussi

# ── Stockage des médias ───────────────────────────────────────────
# `disque` en développement, `r2` en production.
STOCKAGE=disque

# Utilisé uniquement si STOCKAGE=disque. Chemin relatif à apps/studio.
STOCKAGE_DISQUE_RACINE=./medias

# Base d'URL inscrite dans l'artefact publié quand STOCKAGE=disque.
# « / » convient quand les médias sont copiés dans public/ de l'app enfant.
STOCKAGE_DISQUE_URL_PUBLIQUE=/

# Où le studio va chercher les médias qu'il affiche. Laisser vide en local :
# ils passent par sa route /medias. En production, l'URL publique du bucket.
STUDIO_URL_MEDIAS=

# Utilisés uniquement si STOCKAGE=r2. Console Cloudflare > R2 > Manage API tokens.
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=awal-medias

# URL publique du bucket, avec la barre finale. Elle est inscrite dans
# l'artefact publié : c'est par elle que l'app enfant chargera les audios.
R2_URL_PUBLIQUE=https://medias.exemple.com/
